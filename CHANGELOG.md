# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。条目按版本倒序，只记录用户可感知的变更；完整提交历史见 [GitHub commits](https://github.com/kenz1117/dsh-ui-usage-billing/commits/main)。

当前为双线发布：**预览线 1.2.x**（npm `latest` + `alpha`，适配 DSH 0.1.2 ~ 0.1.5 系；宿主 `latest` 现指向 0.1.5-rc.1，插件 `latest` 自 v1.0.26 起跟随宿主 latest 代际，自 v1.2.0 起采用 1.2.x 序列以消除与稳定线的版本号倒挂）与**稳定线 1.1.x**（npm `stable`，适配 DSH 0.1.0-rc.8 ~ 0.1.1-rc.2，维护于 `compat/stable-dsh` 分支；**自 2026-09-11 起冻结**，终版 v1.1.17，不再接收变更）。标签策略与监控机制见 [COMPATIBILITY.md](COMPATIBILITY.md)。

## 预览线（1.2.x，适配 DSH 0.1.2 ~ 0.1.5 系）

> **版本序列说明**：自 v1.2.0 起预览线采用 1.2.x 序列，恒高于稳定线 1.1.x——消除此前 1.0.x < 1.1.x 的版本号倒挂（pnpm `minimumReleaseAge` 冷静期会把刚发布的 latest 跳过、回退到旧稳定线，issue #40 有实测案例）。安装建议钉具体版本号。

### 未发布

- 新增：模型目录收录 **GPT-6 Astra**（OpenAI）——2026-09-03 发布的旗舰模型，官方 id `gpt-6-astra`，按官方标准档定价（缓存输入 $1 / 输入 $10 / 输出 $50，USD 原生币种）。官方另有超长上下文加价（单请求输入 >272K 时整笔输入 ×2、输出 ×1.5）与 Fast 模式 ×2、Batch ×0.5，目录只记标准档
- 新增：模型目录收录 **Kimi K2.8 Preview**（月之暗面）——该型号是 kimi.com 会员侧预览模型，Kimi 开放平台未上架、无官方按量单价（官方文档索引只有 K3 / K2.7 Code / K2.6 / Moonshot V1），目录按 **K2.7 Code 同价估算**（输入 ¥6.5 / 缓存命中 ¥1.3 / 输出 ¥27）并标注「估算价」；日志里各形态 id（`kimi-k2.8-preview` / `kimi-k2.8` / `kimi-k2-8-preview` / `k2.8-preview` / `k2.8`）统一归一化，不再落入「未收录」。走订阅通道时费用记 0（既有机制，与模型无关）
- 文档：README 目录数量校正为 24 厂商 77 款（此前写的 73 款为过时值）；补正月之暗面（9 → 10 款）、智谱 AI（10 → 11 款）、阿里通义（7 → 8 款）、OpenAI（3 → 4 款）款数与 DeepSeek 行型号名（V4 Flash → V4.1 Flash）

### v1.2.5（2026-09-11）

- 适配：宿主 `0.1.5-rc.2`（npm `next` 标签）——`dshReleases` 补逐版本声明。逐包 lib 比对确认 rc.2 相对 rc.1 仅 `client-ui-primitives` 追加代码文件图标 artwork、`client-ui-sidebar` 构建元数据变化，对插件零影响，无需真机重验

### v1.2.4（2026-09-11）

- 修复：侧栏被其他插件抢占挤压时悬浮速览卡跟着挤坏（issue #45）——速览卡宽度此前由 JS 等宽跟随触发卡，侧栏被抢占后触发卡极窄、弹层跟着挤坏；现改为**尺寸固定**：取宿主默认侧栏宽（280）下的计费卡宽（256px）写死在 CSS，与运行时完全解耦——计费卡被挤压收窄时自适应调整自身，悬浮卡始终保持默认宽不受影响（用户拖宽侧栏时也以此默认宽为准）；同时修复订阅悬浮模式弹层在宽侧栏下横跨视口的问题
- 修复：用量页「每日 Token」图的悬停明细卡在最右/最左柱位被裁切（issue #45 反馈）——当日往往最高且在最右，锚点居中的明细卡会推出滚动容器被裁；现按列宽估算边缘列数，安全区外的柱位明细卡向容器内翻转，任意窗口天数（7/14/30）都不再溢出

### v1.2.3（2026-09-10）

- 修复：模型单价表与计费口径对齐官方现行价目页——flash 条目更名 **DeepSeek V4.1 Flash**（官方价目页现行型号 `deepseek-flash`，旧 V4 Flash / Vision (Exp) 已下线并入）；已下线的 Vision (Exp) 不再出现在单价表面板（目录条目与历史回算保留，存量用量照常计价显示）
- 修复：V4 Pro 行在路由分界期（09-10 ~ 09-14 12:00）显示 V4 Pro 刊例（谷 4.5/0.15/13.5、峰×2）而非 Flash 价——单价表显示与实际计费同口径，09-14 12:00 后自动切回 Flash 价
- 文档：单价表提示文案更新（现行型号名、V4 Pro 路由推迟、旧名下线并入）

### v1.2.2（2026-09-10）

- 修复：V4 Pro 路由计费分界从 09-10 12:00 修正为 **09-14 12:00**（官方价目页注释明确「北京时间 2026-09-14 12:00 之后至 V4.1 Pro 上线前，`deepseek-v4-pro` 的请求全部路由到 V4.1 Flash 并按 V4.1 Flash 价格计费」；09-10 邮件公告的时点被官方推迟）——09-10 ~ 09-14 之间 V4 Pro 请求仍按 V4 Pro 刊例计费（谷 4.5/0.15/13.5、峰×2），v1.0.44 起该段被错按 Flash 价折算（低估）；FOLD_VERSION 13 → 14，存量账本行全量重折修正
- 新增：官方价目页新规范模型名 `deepseek-flash`（模型版本 DeepSeek-V4.1-Flash，图像理解并入主线）收录进别名表，计费同 flash 时间线
- 说明：旧模型名 `deepseek-v4-flash` / `deepseek-v4-flash-vision-exp` 官方已下线（仍可调用、由 V4.1 Flash 服务并按 Flash 价计）——与目录现行价一致，历史回算条目全部保留，历史账单不受影响

### v1.2.1（2026-09-10）

- 适配：宿主 0.1.5 系（`latest` 已指向 0.1.5-rc.1）——`dshReleases` 补 0.1.3-alpha.2 / 0.1.5-alpha.1 / 0.1.5-alpha.2 / 0.1.5-rc.1 逐版本声明；宿主 0.1.5-rc.1 真机验证通过（插件加载、用量聚合、历史会话回读）
- 修复：SessionHandle 适配层对 `handle.read` 返回形状的假定——0.1.3-alpha.2 起宿主返回 `SessionHandleReadResult`（`{eventState, events}` 包装）而非裸事件数组，原实现下聚合会静默失败（会话读取全部跳过、统计归零）；现按返回形状探测收窄，0.1.3-alpha.1（裸数组）与 0.1.3-alpha.2+/0.1.5（包装）双形状通吃
- 改进：聚合层持久化面（`UsagePersistence`）改为本地结构声明，不再 `Pick` 自宿主包类型——同一份 bundle 类型检查对所有宿主代际成立，devDeps 后续升级不再被卡

### v1.2.0（2026-09-10）

- 修复：给未收录模型配置自定义价后，「主力消耗模型余额」被兜底 provider（Custom/其他）挤成「未配置」（issue #40 反馈）——单渠道用户现在显示唯一配置成功渠道的余额；多渠道时归属不明则隐藏该行而非误导
- 新增：V4.1 Flash 限时内测端点 id（`deepseek-v4.1-flash-expires-on-0910`）收录进别名表——存量账单的内测用量按 flash 时间线正确归并计费；FOLD_VERSION 12 → 13
- 文档：README 安装命令补 `--profile`（缺失会报 `required option`），并建议钉具体版本号安装

### v1.0.44（2026-09-10）

- 定价：官方公告自 2026-09-10 12:00（北京）起 V4 Pro 的请求路由至 V4.1 Flash 按 Flash 单价计费（V4.1 Pro 上线前）——pro 目录价改写为 Flash 价（谷 1 / 0.02 / 4、峰 ×2），V4 Pro 刊例（峰 9 / 0.3 / 27、谷 4.5 / 0.15 / 13.5）进回算表供分界前事件使用；FOLD_VERSION 11 → 12，存量账本行全量重折（此前分界后 pro 费用按 V4 Pro 刊例折算，高估数倍）
- 新增：`deepseek-v4.1-flash` 正式版模型 id 预防性别名到 flash 键，正式版发布当天日志即正确计费
- 改进：已配自定义价的模型不再出现在「未计价模型」提示条里（issue #43）
- 新增：跨实例提醒认领（issue #44）——移除「检测到另一实例」启动警告（多实例合法、误报频繁），桌面通知改为先到先得认领（`POST /api/billing/notify-claim`，认领表持久化在统计目录旁），同一提醒全机器只弹一次；预算 / 峰谷 / 余额三处通知接入

### v1.0.43（2026-09-08）

- 定价：同步 DeepSeek 官方 2026-09-10 12:00（北京）起的 flash 系调价——V4 Flash / V4 Flash Vision (Exp) 谷档降为 未命中 ¥1 / 输出 ¥4 / 缓存命中 ¥0.02（原 1.5 / 4.5 / 0.05），峰档仍为谷档 × 2。分界前的历史账单按事件时刻回算旧价（与峰谷时代分界同一口径）；FOLD_VERSION 10 → 11，升级后存量账本行全量重折一次（快照先回、不卡界面）
- 修复：用量界面性能表 / 模型 Token 表右端两列数值重叠（issue #41）——子表列宽定义只覆盖 6 列且总和 100%，8/9 列表的后续列在 fixed 布局下宽度为 0，长数值（122141 ms / 426.8M）溢出叠进右列；现首列定宽 + 其余均分、表格最小宽度 640px（窄容器走横向滚动），并对超长值缩写（≥10s 转秒/分、生成速度千位转 k）
- 改进：账单会话明细同标题合并（issue #42）——fork/resume 派生的多段会话共用标题但各自成行，现归并为一行作为一级（调用/费用合计，行尾 ×N 标注段数），二级段明细默认收起、点击一级行展开逐段查看；无标题会话显示「未命名会话 · 短id」不再裸奔短 id
- 优化：客户端 bundle 体积贴着 DSH Store 256 KiB 单文件上限，折叠指示符改由 CSS 伪元素绘制、会话明细渲染收敛，为本版改动腾出空间

### v1.0.42（2026-09-08）

- 新增：`engines.dsh` 兼容声明（`>=0.1.2-alpha.1`，与 `dsh.compatibility` 同步）——DSH Store 按 `engines.dsh` 或同版本线 peer 识别插件与宿主代际的匹配（市场此前读不到 `dsh.compatibility` 自有字段，无法拦截错线安装，issue #40）。注：v1.0.41 的 manifest 因构建事故缺失该字段，请使用 v1.0.42
- 文档：README 快速开始改为按宿主代际分叉的安装命令（0.1.2 系装 `@latest`，0.1.0/0.1.1 系装 `@stable`），并修正安装方式为 `dsh plugin add`

### v1.0.40（2026-09-07）

- 修复：启动 dsh 后会话/模型列表长时间不可用（10~20s 起步，重度用户更久）——`/api/billing/usage-stats` 此前把全量折叠跑在 HTTP 请求里，宿主是单进程单线程，折叠期间事件循环被占住，所有 RPC 一起饿死。现改为 stale-while-revalidate：请求最多等 1.5s，超预算立即回最近一次快照，折叠在后台继续，完成后快照自动刷新
- 修复：重度用户（会话数超过 400 的缓存上限）活跃会话每 30 秒全量重折——账本复用行此前也占用活跃会话的 LRU 名额，把增量折叠状态机挤出缓存；现账本行不进 LRU，活跃会话始终保持增量折叠
- 修复：读不动的会话日志（如新版宿主写入的未知事件格式）每轮聚合都重新解压解析一遍再失败——现按 stamp 负缓存，日志未变不再重读，变更后自动重试
- 修复：账本写放大——任何会话变化都全量序列化重写整个账本文件（上万会话时单次秒级 CPU），现首次立即落盘、之后按 60s 节流，插件卸载时 flush 兜底
- 修复：并发聚合无去重——多个调用方同时触发时各跑一遍全量折叠，现共享同一次进行中的聚合

### v1.0.39（2026-09-07）

- 修复：hover 速览卡在部分桌面壳下溢出计费卡宽度（issue #37）——速览卡此前寄生在侧栏容器内部，宿主对容器的样式处理都可能波及它：`container-type` 使容器成为 fixed 后代的包含块（坐标被二次偏移）、宿主的 box-sizing 重置不覆盖弹层（内联 width 被当内容宽，再加 padding/border 后实际宽度超出计费卡 26px）。现在弹层经 React portal 挂载到页面根节点：真正相对视口定位、自带 box-sizing 重置与独立层叠层级，不再依赖侧栏容器的任何样式环境
- 修复：速览卡与触发卡之间的 hover 断档——弹层独立于触发卡 DOM 后，鼠标移向弹层会先触发触发卡 mouseleave 而闪断；现以 120ms 关闭延迟互相桥接，鼠标在两者间移动不闪烁

### v1.0.38（2026-09-07）

- 修复：速览卡向上弹出被侧栏容器 `overflow: hidden` 几何裁剪（issue #37，dsh-desktop 桌面壳）——超出容器的部分被直接裁掉，任何 z-index 无法对抗。定位改为 `fixed`，坐标按触发卡视口位置实时计算（悬停进入、侧栏滚动、窗口缩放时重算），脱离侧栏裁剪上下文，在任意桌面壳/宿主下完整显示

### v1.0.37（2026-09-07）

- 修复：「本周」实为近 7 天滚动窗口（issue #39）——周一出现「本周 ¥253.0M ≠ 今日 ¥86.0M」的口径错位。现按自然周（周一起算）统计；稳定线 v1.1.8 同步修复

### v1.0.36（2026-09-07）

- 修复：计费面板模型子表列位漂移（issue #37）——「模型/调用/输入/输出/缓存命中率/实际」六列此前按各组内容自动分配宽度，跨组纵向不对齐；现改为固定布局 + 统一列宽（模型列 30%，数字列 12–15% 右对齐），各厂商组的表格纵向对齐成同一网格
- 修复：订阅豁免行（Grok 等）被计入组头费用（issue #37）——豁免通道实际费用为 ¥0，组头小计不再含其按目录价的估算

### v1.0.35（2026-09-07）

- 修复：计费面板厂商组头「费用/套餐/余额」三槽列位漂移（issue #37）——三槽固定最小宽度（费用/余额 118px 右对齐，套餐徽标 64px 空占位保列位），跨组纵向对齐

### v1.0.34（2026-09-07）

- 新增：识别订阅管理插件（dsh-plugin-subscrip）注册的订阅直连通道——provider id `grok`（X Premium 订阅）加入默认订阅豁免与显示名映射，其调用自动归位「直连 · Grok（X Premium）」且不再按 token 误计费（issue #37）；配置指纹变化使历史聚合自动重折
- 修复：hover 速览卡弹出时可能被侧栏会话列表盖住（issue #37，桌面壳）——悬停/聚焦时抬升计费卡层叠，速览卡完整浮于列表之上

### v1.0.33（2026-09-07）

- 新增：订阅豁免命中的配置外通道归位 `direct` 桶——grok build 等经订阅管理插件注册的通道不经 llm-pi-ai 路由表，此前落「未知路由」且按 token 误计费（issue #37）。升级后在插件配置 `subscriptionProviders` 中加入对应 provider id 即可豁免计费并归位通道，历史聚合随配置指纹变化自动重折
- 新增：聚合配置指纹进账本复用判定——订阅豁免/官方名单/路由别名/搜索估值的任一变更都会使历史账本行失效并全量重折，配置变更即时生效（此前改配置后旧聚合永远沿用）；指纹不一致且日志已删的行标注 stale
- 兼容：v1.0.30 的官方直连归位与 v1.0.32 的速览卡隐藏加固不受影响

### v1.0.32（2026-09-07）

- 修复：侧栏计费卡的 hover 速览卡可能在冲突插件环境下以空壳常显（issue #37）——显隐原只靠 `opacity`，外部全局样式可把它拉回可见。现改为 `opacity + visibility` 成对控制，`visibility: hidden` 不渲染、不挡视线，外部覆盖不再能破坏隐藏态

### v1.0.31（2026-09-05）

- 修复：侧栏计费卡「漂到侧栏中部」（issue #36）——`dsh-cpa-status` / `dsh-mcp-connector` 会用全局 `!important` 把 `footerActions` 容器改成纵向 flex，1.0.26 起计费卡的 `flex-basis: 180px` 在纵向布局下语义变成 180px 高度，卡片被拉高脱离底部。尺寸语义改为 `width: 100%`（两个方向下都表示占满可用宽度，高度恒由内容决定）；与刚性兄弟胶囊横向共存时的收缩保护（96px 地板 + 容器查询分档降级，issue #32）保持不变

### v1.0.30（2026-09-05）

- 修复：官方直连费用被错归「未知路由」（v1.0.29 回归，issue #34 后续）——宿主内置官方直连不经 llm-pi-ai 路由表，provider 名 `deepseek-official` 不在任何配置里，v1.0.29 的「未知路由一律不算官方」把它整体错杀进三方桶。现对 `deepseek-*` 形态的配置外路由按名兜底归位 `direct` 并计官方渠道；前端同显示名通道合并（`direct:deepseek` 与 `direct:deepseek-official` 同显「DeepSeek 官方」）
- 折叠算法版本 10：除上述归位语义外，账本行开始持久化会话标题（此前 `serializeFold` 白名单漏了 `title`，重启后从账本复用的会话明细只剩 id 前缀）；v9 及更早的账本行触发一次性全量重折补回标题
- 注意：若聚合结果曾被旧实例污染，v10 重折会自动纠正；极端情况下可手动删除 `~/.dsh/.dsh-usage-ledger.json*` 后重启宿主强制重建
- 优化：会话明细按工作区分组展示（组行内联合计），吸收原「工作区统计」卡片

### v1.0.29（2026-09-05）

- 新增：提供商（通道）优先分组——计费区块按调用实际发生的 llm 入口分组（腾讯云 TokenHub / 腾讯云 Token Plan / DeepSeek 官方 / 直连·路由名 / 未知路由），模型品牌保留为行内徽标 + 副标；订阅卡与余额按通道挂接，官方判定改为按通道 origin（`api.deepseek.com`）而非路由名
- 新增：腾讯云 Token Plan 订阅额度适配器（`tencent-token-plan` 路由 + 云 API 密钥对 `<SecretId>:<SecretKey>`；TC3 管控面 `DescribeTokenPlanList → DescribeTokenPlan` 链路抽到 `src/tc3.ts`，与余额面板共用）；仅剩余与总额度都可解析时产出百分比窗口，绝不猜总额度
- 新增：订阅通道模型行显示「订阅包含 ≈目录价预估」，Token Plan 的逐模型开销可估
- 新增：`routeAliases` 配置——改名/删除的历史路由按别名归位（站点/订阅/官方判定同步生效）；`modelKeyAliases` 配置——目录外模型 id 手动绑定计费键
- 修复：模型 id 归一化覆盖日期后缀（`deepseek-v4-flash-202605`）、组织前缀（`deepseek/deepseek-v4-flash`）与 TokenHub 短 id（`hy3`）等形态；目录键本身含数字段（`mistral-large-2512`）不受影响
- 修复：名为 `deepseek-*` 的中转/网关路由曾被按路由名误判为官方渠道（腾讯网关的 DeepSeek 全算官方流量）
- 修复：订阅识别两套口径不一致——订阅卡已识别 `tencent-token-plan` 而聚合豁免未包含，现加入默认豁免集合
- 折叠算法版本 8：上述归属语义变化触发历史日志一次性全量重折（价格仍按事件时刻口径，不重算历史价格）；已删除会话的历史行保留并标记 stale

### v1.0.29-alpha.1（2026-09-05，npm `alpha`）

- 兼容 DSH 0.1.3-alpha.1 的 SessionHandle 持久化模型：宿主 0.1.3 起读取改为 `open(id,'read') → handle.read(offset)`、`list` 返回快照行（`readFrom`/`locate` 消失），注入点按结构探测把两种宿主形状收敛为聚合层的 0.1.2 面貌；revision 令牌经 `stampOf` 暴露，增量折叠在 0.1.3 上照常生效
- 声明 `0.1.3-alpha.1` 兼容（宿主 npm 尚未发布该版本，真机验证待其上 npm 后补做）
- 对 0.1.2 系宿主行为零变化（探测直通）；session format v2 的逻辑事件对折叠面天然兼容（`assistant/chunk` 嵌入 message、新增 `assistant/attempt` 由既有运行时收窄安全跳过）
### v1.0.28（2026-09-05）

- 修复：峰谷倒计时未跳过周末全天空闲——周五深夜（工作日 18:00 后）把周六 09:00 伪边界当「转峰」时刻，与跨零点后显示的周一 09:00 矛盾（issue #33）。下一切换点统一定义为档位真正变化的最近边界，由含周末全谷规则的 `tierAt` 驱动扫描；峰谷切换预告（`upcomingTierSwitch`）同步修正，周末不再误报预告

### v1.0.27（2026-09-04）

- 「平价消耗胶囊」位置设置改为下拉选择（沿用宿主 Menu 原语，显示当前选中项），选项扩展为三档：输入框下方（默认）/ 输入框上方 / 输入框内部（宿主 input.right 座位的内联 chip：档位 + 会话费用恒定占位，完整信息走悬浮提示，额度预警变色不加宽）；输入框上方时胶囊与下方一致水平居中；精简设置卡描述文案
- 修复 zh 词典缺失条目导致设置文案回退英文 key；新增 zh/en 键集一致性测试防线

### v1.0.26（2026-09-04）

- 修复：自定义单价卡改为两级布局（行1 模型/来源/币种；标准价/低谷价为缩进的二级行），删除改为勾选 + 底部「删除所选」（issue #32）
- 修复：侧栏胶囊在与他插件并列时被挤压成不可读残段；现按 180px 基准公平分配并有 96px 可读地板（issue #32）
- 声明 DSH `0.1.2-rc.1` 兼容；新增 `dsh` SemVer 区间声明（`>=0.1.2-alpha.1`），市场可按区间拦截不匹配宿主（issue #31 根因修复）
- devDependencies 的 `@deepseek-ai/dsh-*` pin 升级至 `0.1.2-rc.1`
- 新增 [COMPATIBILITY.md](COMPATIBILITY.md)（双线兼容单一事实源）、`scripts/check-compat.mjs`（矩阵校验脚本）、`watch-dsh-releases` 每日监控 workflow（漂移自动开 `compat-drift` issue）
- 文档：README 兼容段改为双线指引；英文 README 补齐 DSH STORE 权限与兼容声明章节

### v1.0.25（2026-09-04）

- 修复：活跃会话按 seq 增量折叠，不再每轮全量重读日志，消除大会话下会话列表长时间不加载（issue #30）
- 全量重折路径分片执行并让出事件循环；账本行一致时零读取；附增量/全量折叠等价性测试

### v1.0.24（2026-09-02）

- 声明 DSH 0.1.2-alpha.4 / alpha.5 兼容
- 修复：月度预计按预测月自身天数计算，而非当前月

### v1.0.23（2026-09-02）

- 修复：fork 会话的种子边界改用持久化的 seedLength（issue #29）

### v1.0.22（2026-09-01）

- 修复：设置命名空间校验内联化，避免宿主差异导致启动失败（issue #28）

### v1.0.21（2026-08-31）

- 新增：内置 TokenDance Space 钱包余额适配器（issue #27）

### v1.0.20（2026-08-31）

- 修复：自定义余额请求头中任意位置的 `{{ENV}}` 凭据占位符均可解析

### v1.0.19（2026-08-30）

- 修复：2026-08-22T16:00Z 之前的周末按峰谷 v1 窗口计价
- 修复：厂商 SVG 图标 data URI 精度回归
- 性能：bundle 瘦身审计，裁剪 locale key 前缀并新增 Store 体积门禁

### v1.0.18（2026-08-30）

- 新增：性能面板按模型对比曲线与悬停吸附十字线（PR #24，@hi-fangj）
- 修复：峰谷计价生效前的 DeepSeek 用量按官方基础价计费
- 界面：数据面板密度优化；仪表盘重设计落地（Hero 52px、环比徽章、分段 Tab）

### v1.0.17（2026-08-30）

- 新增：即时代费用条（平价消耗胶囊）显示开关（PR #22，@hi-fangj）
- 新增：Token 每日图按模型视角与图例聚焦（PR #23，@hi-fangj）
- 界面：设置 Tab 分组网格化；KPI 标签大写、数值列右对齐

### v1.0.16（2026-08-30）

- 修复：npm 发布清单损坏；client bundle 重建
- 宿主无 `remote.llm` 服务时优雅降级

### v1.0.15（2026-08-29）

- 移除：中转站分布面板

### v1.0.14（2026-08-29）

- 新增：Token 每日图悬停显示精确逐日明细（PR #21，@hi-fangj）

### v1.0.13（2026-08-29）

- 自定义单价可靠性修复；低谷时段分带；腾讯云 TokenHub Token Plan 余量接入（TC3 签名）

### v1.0.12（2026-08-29）

- 迁移至 DSH 0.1.2-alpha.1；0.1.1 系宿主请使用稳定线 1.1.x 或固定 v1.0.11

### v1.0.11（2026-08-29）

- 厂商 Logo 瘦身以满足 DSH Store 256 KiB 体积上限；0.1.1 系宿主的最后一个 1.0.x 兼容版

### v1.0.10（2026-08-28）

- 新增：结构化自定义单价表，支持按中转站来源绑定同模型不同价

### v1.0.9（2026-08-28）

- 新增：联网搜索请求按次费用估算（`searchCallEstimateCny`，issue #15）
- 新增：真实档位分桶、工具排行、逐轮耗时
- 修复：用量按消息自身来源归属（issue #14）；账本行标记 foldVersion

### v1.0.3 ~ v1.0.6（2026-08-27）

- 通义千问家族官方定价、推广徽章、费率表剔除 models.dev 补充项、费用/Token 显示切换
- 通过 DSH Store `build-dsh-plugin` 契约审计与体积审查（AI-Scarlett/DSH-Store#255）

### v1.0.0 ~ v1.0.2（2026-08-25）

- 仪表盘重设计；MiniMax-M2.7 目录；模型目录扩容、人民币定价、厂商 Logo
- GLM 家族目录 + models.dev 自动抓价

## 稳定线（1.1.x，适配 DSH 0.1.0-rc.8 ~ 0.1.1-rc.2）

### v1.1.5（2026-09-04）

- 回填预览线的 issue #30 修复：seq 增量折叠 + 分片让出，消除会话列表阻塞

### v1.1.0（2026-08-31）

- 0.1.1 系宿主的稳定兼容线首个版本：backport 计费时间线修正（历史账单按官方分界价重算）与界面密度更新
- 中间版本（1.1.1 ~ 1.1.4）为同期预览线修复的回填，详见 `compat/stable-dsh` 分支提交

## 早期版本（0.x）

### v0.9.2 ~ v0.9.20（2026-08-22 ~ 08-25）

- 周末全天低谷计价、设置 Tab、多套餐额度、峰谷切换提醒（0.9.2）
- MiniMax 国内域 Token Plan（PR #5/#6，@ciphoo）；中转站归组与额度、智谱余额、工作区项目（0.9.8）；xAI 余额；Token 统计面板
- 会话删除后用量保留的持久账本（PR #8，@fabulousyuann-tech）；Windows 账本并发写入修复（PR #11，@ciphoo）；minimax-cn 凭据接线（PR #12，@ciphoo）
- 声明端点 + 余额差对账（0.9.18）；未计价模型提示、回环校验、中转站诊断、按站点导出（0.9.10）

### v0.4.0 ~ v0.6.x（2026-08-21 ~ 08-22）

- 分厂商计费与订阅面板、模型健康、热力图、每轮费用、工作区统计、双币种（0.4.0）
- Moonshot/Kimi、阶跃星辰余额查询；玻璃拟态仪表盘重设计（0.6.0）；数据导出、角色归因、分档预算提醒、`usage_stats` 工具

### v0.3.0（2026-08-21）

- 预算条与超支提醒、会话面板、按日堆叠趋势、余额可撑天数与低余额提醒、未收录模型反馈

### v0.2.2 ~ v0.2.6（2026-08-16）

- Hero 重设计、7 天分组趋势、实时价/汇率；余额列与自动刷新；Kimi K3 编码套餐归属修复；小米 MiMo 目录与订阅判定

### v0.1.0（2026-08-16）

- 首个版本：订阅 provider 豁免 + 人民币计价
